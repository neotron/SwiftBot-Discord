{
    "command_prefix": "~",
    "database_directory": "/path/to/persistent/storage/ - used for SQLite database",
    "auth": "the bot auth token",
    "owner_ids": [
	"array of owner ids",
	"Owners are super admins",
	"get the id's using the 'id' command"
    ]
}
